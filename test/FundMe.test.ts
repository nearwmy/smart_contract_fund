import hre from "hardhat";

describe("test fundme contract", async () => {
  it("test if the owner is msg.sender", async () => {
    // const fundmeFactory = await hre.ethers.ContractFactory();
  });
});
